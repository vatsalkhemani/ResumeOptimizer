import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  variable: "--font-sans",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Resume Optimizer | Turn Your Resume Into a Job Magnet",
  description: "Upload your resume and get AI-powered suggestions to optimize it for any job description. ATS-friendly, professional output.",
  keywords: ["resume", "optimizer", "ATS", "job search", "career", "AI"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={inter.variable}>
        {children}
      </body>
    </html>
  );
}
